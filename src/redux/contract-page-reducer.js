import {createSlice} from "@reduxjs/toolkit";

export const contractPageSlice = createSlice({
    name: "ContractPage",
    initialState: {

    },
    reducers: {
    }
})

//export const {  } = contractPageSlice.actions;
export default contractPageSlice.reducer;