import {createSlice} from "@reduxjs/toolkit";

export const mainPageFormSlice = createSlice({
    name: "MainPage form",
    initialState: {

    },
    reducers: {
    }
})

//export const {} = mainPageFormSlice.actions;
export default mainPageFormSlice.reducer;